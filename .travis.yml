language: php

php:
  - 5.3
  - 5.4
  - 5.5
  - 5.6
  - 7.0
  - hhvm

matrix:
  fast_finish: true
  allow_failures:
    - php: hhvm

mysql:
  database: cloudbooks
  username: root
  password: root
  encoding: utf8

before_install:
  #- sudo apt-get update > /dev/null

install:
  - export PATH="$HOME/.composer/vendor/bin:$PATH"

before_script:
  - composer install -vvv
script: phpunit
